---
layout: layouts/layout.liquid
templateClass: post
---
<h1>{{ title }}</h1>
{% if subtitle %}
<p class="subtitle">{{ subtitle }}</p>
{% endif %}

{{ content }}

<p class='footer'>
    <span class="badge"><a href="/">← Home</a></span>
</p>